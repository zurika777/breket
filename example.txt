.length - რაოდენობას დათვლის -სიგრძე
for -ში i ნიშნავს იგივე index -ს
return  -დაბრუნების დროს ფუნქცია წყდება მთავრდება
document არის მთლიანად Html - ი რაც იქმნება ან რასაც შენ ქმნი
Date(); თარიღის ობიექტი და არა სტრინგი
indexOf ნეით მეთოდი
slice ნეით მეთოდი
filter ნეით მეთოდი - ფილტრაციის ფუნქცია
value მნიშვნელობა -სახელი
key  რიგის ნომერი -  რიგითობა
map უვლის თითოეულ ელემენტს დაახლოებით იგივე for ია
Метод getElementById()возвращает элемент с указанным значением.
innerHTML Получить HTML-контент элемента с id ანიჭებს ან აბრუნებს HTML ელემენტის  შინაარსს (შიდა HTML).
innerText задает или возвращает текстовое содержимое элемента.
appendChild() добавляет узел (элемент) в качестве последнего дочернего элемента элемента.
let demo = document.getElementById("demo") ანუ ვიპოვოთ ელემენტი დასახელებით "demo"
entrie ჩანაწერი-ჩანაწერები
DOM: Document, Object, Model არის ინტერფეისი სრული რუკა ჩვენი ელემენტების
BOM: Browser, Object, Model არის window არის ის ობიექტი რომელიც შეიცავს თვისებას ბრაუზერზე window და document არ არსებობს ჯავასკ რიპტში
თიოეულ ელემენტს ქვია ნოდი
თითო ელემეტი არის ობიექტი იმიტომ რომ შეიცავს ძალიან ბევრ თვისებებს
${} სინტაქსისია გამოყოფის
ნეით არის ფუნქცია შემოკლებული
Метод setAttribute()устанавливает новое значение атрибута.
let text = "Hello World!";
Object არის თით ელემენტი არის ობიექტი და შეიცავს საკუთარ თავზე ინფორმაციას სადაც შედის თვისებებიც ივენთებიც, მეთოდებიც
Model ნიშნავს რომ ობიექტის მოდელით მუშაობს
google profiler ეს არის გამოაქვს მაგალითები ექსტენშენია programis
get წამოიღე რომელიღაცა კეი
exists თუ არსებოსბ ეს კიი წამოიღე
getJSON იგივე get - ია უბრალოდ ჯეისონის ფორმატში გადაგვაქვს ეს მონაცემები
set არის რაღაცის ჩადეება- დამატება  მაგ LocalStorage - ში
remove არის წაშლა
clear მთლიანად გასუფთავება
e.stopPropagation() // აჩერებს პილველ მოქმედებაზე მაგ : თყუ ათჯერ დააჭირა კლიკს არ დაიდება ათჯერ ზედიზედ ერთიდაიგივე პოსტი
e.preventDefault()
JSON.stringify(), чтобы преобразовать его в строку. სტრიქონად.
Метод push()добавляет новые элементы в конец массива.
Метод push()изменяет длину массива.
Метод push()возвращает новую длину.
Метод prepend() вставляет указанное содержимое в начало выбранных элементов.

function createElelemt() {  ფუნქციის სახელი შექმენი ელემენტი
    let element = ducument.createElement("p"); ცვლადი უდრის შექმნას ელემენტი p ანუ პარაგრაფი
    element.innerHTML = text;  ვანიჭებთ ამ ელემენტს text - ს ანუ "Hello World!" -ს
    demo.appendChild(element);''
}
createElelemt(); ქმნის ელემენტს
value - saxeli

function createElement(cvladi, elementi) {
    elementi  პარაგრაფი p ან button
    createElement('მაკა' , 'p')
    item  - ნივთი.  არის უბრალოდ სახელი მეტი არაფრი










